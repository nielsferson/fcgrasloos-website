{
  "name": "fcgrasloos-scores-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'no build step required for this Worker'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
